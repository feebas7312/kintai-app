<div class='wrapper'>
  <%= render 'shared/side_bar' %>

  <div class='main'>
    <div class='main-header'>
      <h2 class='page-title'>スタッフ一覧</h2>
    </div>

    <div class='main-contents'>
      <div class='admin-content'>
        <div class='admin-content__block'>
          <div class='content-header'>
            <div class='number'>社員番号</div>
            <div class='name'>名前</div>
            <div class='joining-date'>入社日</div>
          </div>
          <div class='member-content'>
            <div class='number'><%= link_to @admin.number, admins_show_path %></div>
            <div class='name'><%= @admin.last_name %><%= @admin.first_name %></div>
            <div class='joining-date'><%= @admin.joining_date %></div>
          </div>
        </div>
        <div class='edit-btn'>
          <%= link_to '編集', edit_admin_registration_path, class: 'btn' %>
        </div>
      </div>
      <% @employees.each do |employee| %>
        <div class='employee-content'>
          <div class='member-content'>
            <div class='number'><%= link_to employee.number, employees_show_path(employee_id: employee.id) %></div>
            <div class='name'><%= employee.last_name %><%= employee.first_name %></div>
            <div class='joining-date'><%= employee.joining_date %></div>
          </div>
          <div class='edit-btn'>
            <%= link_to '編集', edit_employee_registration_path(employee_id: employee.id), class: 'btn' %>
          </div>
        </div>
      <% end %>
    </div>

    <div class='form'>
      <%= link_to 'スタッフを追加', new_employee_registration_path, class: 'btn' %>
    </div>
  </div>
</div>